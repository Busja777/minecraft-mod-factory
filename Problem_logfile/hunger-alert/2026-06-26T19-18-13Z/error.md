# Pipeline Failure — hunger-alert

**Timestamp:** 2026-06-26T19-18-13Z
**Run URL:** https://github.com/Busja777/minecraft-mod-factory/actions/runs/28259947766
**Trigger:** workflow_dispatch

## What failed
Check the run URL above for the full step-by-step log.

## Generated Mod Info
```json
{
  "name": "HungerAlert",
  "modId": "hunger-alert",
  "description": "Warns players with a chat message and action bar alert when their hunger or health drops to a critical level, helping them react before it's too late.",
  "changelog": "## v1.0.0\n- Initial release\n- Action bar warning when hunger drops to 6 or below (🍗 Low Hunger!)\n- Action bar warning when health drops to 6 or below (❤️ Low Health!)\n- Alerts throttled to once every 30 seconds per player to avoid spam\n- Fully server-side, no client mod required",
  "javaCode": "package com.factory.mod;\n\nimport net.fabricmc.api.ModInitializer;\nimport net.fabricmc.fabric.api.event.lifecycle.v1.ServerTickEvents;\nimport net.minecraft.server.network.ServerPlayerEntity;\nimport net.minecraft.text.Text;\nimport net.minecraft.util.Formatting;\n\nimport java.util.HashMap;\nimport java.util.Map;\nimport java.util.UUID;\n\npublic class ModMain implements ModInitializer {\n\n    private static final String MOD_ID = \"hunger-alert\";\n\n    // Thresholds (out of 20 points; 6 = 3 hunger/hearts)\n    private static final int HUNGER_THRESHOLD = 6;\n    private static final float HEALTH_THRESHOLD = 6.0f;\n\n    // Cooldown: 30 seconds × 20 ticks = 600 ticks\n    private static final int COOLDOWN_TICKS = 600;\n\n    // Tracks the last tick each player received a hunger alert\n    private final Map<UUID, Long> lastHungerAlert = new HashMap<>();\n    // Tracks the last tick each player received a health alert\n    private final Map<UUID, Long> lastHealthAlert = new HashMap<>();\n\n    @Override\n    public void onInitialize() {\n        ServerTickEvents.END_SERVER_TICK.register(server -> {\n            long currentTick = server.getTicks();\n\n            for (ServerPlayerEntity player : server.getPlayerManager().getPlayerList()) {\n                UUID id = player.getUuid();\n\n                // ── Hunger Check ──────────────────────────────────────────\n                int foodLevel = player.getHungerManager().getFoodLevel();\n                if (foodLevel <= HUNGER_THRESHOLD) {\n                    long lastAlert = lastHungerAlert.getOrDefault(id, -COOLDOWN_TICKS);\n                    if (currentTick - lastAlert >= COOLDOWN_TICKS) {\n                        lastHungerAlert.put(id, currentTick);\n\n                        // Action bar message (overlay — non-intrusive)\n                        player.sendMessage(\n                            Text.literal(\"🍗 Low Hunger! Eat something soon!\")\n                                .formatted(Formatting.GOLD, Formatting.BOLD),\n                            true  // true = action bar\n                        );\n                    }\n                } else {\n                    // Reset cooldown once hunger recovers so it can warn again quickly\n                    lastHungerAlert.remove(id);\n                }\n\n                // ── Health Check ──────────────────────────────────────────\n                float health = player.getHealth();\n                if (health <= HEALTH_THRESHOLD) {\n                    long lastAlert = lastHealthAlert.getOrDefault(id, -COOLDOWN_TICKS);\n                    if (currentTick - lastAlert >= COOLDOWN_TICKS) {\n                        lastHealthAlert.put(id, currentTick);\n\n                        // Action bar message\n                        player.sendMessage(\n                            Text.literal(\"❤️ Critical Health! Find cover!\")\n                                .formatted(Formatting.RED, Formatting.BOLD),\n                            true  // true = action bar\n                        );\n                    }\n                } else {\n                    // Reset cooldown once health recovers\n                    lastHealthAlert.remove(id);\n                }\n            }\n        });\n    }\n}\n",
  "imagePrompt": "A dramatic Minecraft-style gaming thumbnail with a dark atmospheric background. In the center, a detailed pixel-art Steve character looks panicked, with glowing red hearts nearly empty and golden hunger drumsticks almost depleted shown as large HUD icons floating beside him. Bold golden text reads \"HungerAlert\" at the top with a fiery glow effect. A red action-bar warning banner floats across the screen reading \"❤️ Critical Health! Find cover!\" in bright white letters. The background shows a dark cave environment with glowing lava pools. Minecraft block textures, vibrant colors, dramatic lighting, CurseForge mod thumbnail style."
}```

## Gradle Build Report
_See Problem_logfile/hunger-alert/2026-06-26T19-18-13Z/build-error.txt_
